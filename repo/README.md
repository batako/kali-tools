# Published APT Repository Root

This directory is the document root for AWS Amplify static hosting.

Only files under `repo/` are published. Go source code, tests, and packaging sources remain outside of the published output.

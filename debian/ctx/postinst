#!/bin/sh

set -e

if [ "${1:-}" = "configure" ]; then
  cat <<'MSG'
ctx installed successfully.

Recommended:

    ctx init-shell

This enables shell completion for your current shell.

To also enable the optional pj, ta, cr, and sv shortcuts:

    ctx init-shell --extra-shortcuts
MSG
fi

exit 0

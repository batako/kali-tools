# xgobuster 1.2.0

`xgobuster 1.2.0` は、HTTPサービスが複数ある場合の接続先選択を改善します。

## 追加

- 表示された番号でWebサービスを選択する`--service <番号>`を追加します。
- Webサービス番号のBash/Zsh補完を追加し、Zshではサービスの説明も表示します。

## 変更

- `--service`を指定しない場合は、従来どおり対話的にサービスを選択します。
- 存在しないサービス番号を拒否し、`--service`と`--url`の併用を禁止します。

# xssh 1.2.0

`xssh 1.2.0` improves first-time SSH connections and terminal handling for interactive sessions.

## Changed

- Automatically trusts a new host key on first connection while still rejecting changed keys for known hosts.
- Connects interactive SSH standard streams directly to the terminal.
- Stores only execution metadata and completion status in command logs, not SSH session contents.

# req 1.0.0

`req 1.0.0` は、TLS検証の制御とパッケージインストールに対応したリリースです。

## 機能

- 検証環境向けにTLS証明書検証を無効化する`-k`/`--no-tls-validation`を追加します。
- TLS証明書検証を明示的に有効化する`--tls-verify`を追加します。

## 使い方

```sh
req request.req
req -k request.req
```

`req` はデフォルトでTLS証明書を検証します。期限切れや自己署名証明書を使う検証環境だけ`-k`を使用してください。

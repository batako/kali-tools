# xwebshell 1.0.1

`xwebshell 1.0.1` aligns reverse-shell callback IP detection with the local IP shown by the ctx shell prompt.

## Updated

- Use `ctx prompt` local IP data for callback IP configuration.
- Prompt for the callback IP only when ctx cannot provide one.

# ctx 1.3.0

ctx 1.3.0 は、Webコンテンツ探索連携のためのデータ基盤を追加します。

## 追加

- `web_discoveries` テーブルとDBマイグレーションを追加
- Web探索結果を追記保存・一覧取得するAPIを追加
- Webコンテンツ用のデフォルトワードリストを設定する `web.wordlist` を追加
- 探索実行とcommand logを関連付けて保存

このリリースでは `xgobuster` コマンド自体は追加していません。Gobuster連携は、この基盤を使って別途実装します。
